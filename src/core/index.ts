/**
 * Core Module Index - GeminiHydra
 *
 * NOTE: This barrel file is intentionally kept minimal.
 * All internal code should import directly from specific modules
 * (e.g., './agent/Agent.js', './swarm/Swarm.js').
 *
 * The public API is exported from src/index.ts which imports
 * directly from specific core modules.
 */

// No re-exports - import from specific modules directly
